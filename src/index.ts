import { WebAssemblyGenerator } from "./generator";
import { WebAssemblyType, WebAssemblyIntegerType, WebAssemblyFloatingType } from "./types";

export {
    WebAssemblyGenerator,
    WebAssemblyType,
    WebAssemblyIntegerType,
    WebAssemblyFloatingType
};